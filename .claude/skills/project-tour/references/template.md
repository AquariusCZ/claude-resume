# <项目名> — AI 导览(AI_GUIDE.md)

> 一句话:<这个项目做什么、给谁用、解决什么问题>。
> 本文件供 AI **只读问答**优先加载:80% 的常见技术问题看这里就能答;深挖时见文末「文档索引」。

## 1. 定位
- **用途**:<一段话>
- **使用者 / 场景**:<谁、在什么场合用>
- **技术栈**:<语言 / 关键库 / 依赖的仪器或硬件>

## 2. 架构与数据流
<用 ASCII 图画出「输入/采集 → 处理 → 输出(图/报告/落盘)」的主链路;标出关键进程、线程、通信方式(TCP/串口/DLL/子进程等)。>

```
<例:上位机 GUI ──启动──▶ 各测试脚本(子进程) ──SSE──▶ 汇总日志/实时曲线
                          │
                          ├─ SMU(源表)  ─ctypes DLL─▶ 硬件
                          └─ 激光器/示波器 ─VISA─▶ 硬件>
```

## 3. 模块职责(路径 → 职责 → 关键函数/入口)
| 路径 | 一句话职责 | 关键函数 / 入口 |
|---|---|---|
| `xxx.py` | | |
| `common/` | | |
| `scripts/` | | |

## 4. 测试 / 运行流程
- **入口脚本**:<文件 + 如何启动>
- **跑一次测试**:<命令 + 关键参数>
- **依赖 / 仪器 / 环境**:<Python 版本、库、需要连的仪器、驱动>
- **典型流程**:<1) … 2) … 3) …>

## 5. 数据格式与命名约定
- **目录布局**:<数据落在哪、怎么组织>
- **文件名解码**(逐字段;放一个真实样例):
  `<真实文件名样例>` 拆解为:
  - `<字段1>` = <含义>
  - `<字段2>` = <含义>
  - …
- **数据文件内部结构**:<如 .mat 的键 / CSV 列 / JSON 字段>

## 6. FAQ(同事高频问题,直接给答案)
- **Q:<最常被问的问题>** — A:<直接答案,必要时指到具体文件/函数>
- **Q:** — A:
- **Q:** — A:
<写 5–10 条,覆盖"整体是什么 / 测试怎么跑 / 某个数据怎么来的 / 某模块干嘛的 / 常见报错">

## 7. 术语表(中英 / 缩写对照)
| 术语 | 含义 |
|---|---|
| | |

## 8. 文档索引(深挖时读)
- [docs/xxx.md](docs/xxx.md):<这篇讲什么、什么问题该看它>
- [README.md](README.md):<>
