# P0 基线快照(2026-08-06 夜)

任务书 `docs/OVERNIGHT-HARDENING-BRIEF.md` 的动手前三项确认 + P0 DoD 快照。

## 前置确认(任一不符即停手——实际全部符合)

| 项 | 期望 | 实测 |
|---|---|---|
| git HEAD | `41ec715` | `41ec715957e3d60ad7163b632ea7d6eec63accdf`(分支 `s2-external`,即任务书自身提交 S10-O) |
| 工作树 | 干净 | `git status --short` 输出为空 |
| 测试基线 | 443 通过 / 0 失败 | `cd csharp && dotnet test -v q --nologo` → **通过 443 / 失败 0 / 跳过 0**,耗时 28s |
| cc-connect 进程 | 恰好 1 个 | PID **40996**,启动 2026-08-06 11:17:52,路径 `AppData\Roaming\npm\node_modules\cc-connect\bin\cc-connect.exe` |

## 不变量(P6 必须逐一复核)

- **cc-connect PID**:40996(启动 2026-08-06 11:17:52)。整夜结束时必须仍是它。
- **config.toml SHA256**:`~/.cc-connect/config.toml` = `22337F1EF0436BD93092BDF09277CFD4C058EFF8ACDA239340512445ACFFC024`(只算哈希,未读内容)。
- **git HEAD(工作起点)**:`41ec715`,工作分支 `s10-overnight-hardening` 从它切出。
- **测试基线**:443 / 0 / 0。只允许变大,不允许变小。

## 现场备注

- 任务书 §2 写的是从 `a120410` 切分支,但用户指令与现场 HEAD 均为 `41ec715`(S10-O,
  即任务书本身入库的提交)。按任务书 §4「发现任务书本身和现场对不上 → 以现场为准」,
  从 `41ec715` 切分支。
- 全量测试期间 cc-connect 未被触碰;测试的管道隔离(AIRESUME_TEST_PIPE_SUFFIX GUID
  后缀)与生产 Worker 无冲突,443 全绿复现了任务书 §0.5 的既有结论。
